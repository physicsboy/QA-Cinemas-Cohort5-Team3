package com.qa.cinema.persistence;



import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;

import com.qa.cinema.persistence.Media;
import com.qa.cinema.persistence.MediaType;


public class MediaTest {
	
	//TODO
	
	/*Media media;
	String url = "Some/url";
	MediaType type = MediaType.POSTER;

	@Before
	public void setUp() {
		media = new Media();
		media = new Media(url, type);
	}

	@Test
	public void testGetID(){
		assertNull(media.getId());
		media.setId(new Long(1));
		assertEquals(new Long(1), media.getId());
	}
	
	@Test
	public void testGetUrl(){
		assertEquals(url, media.getUrl());
		media.setUrl("newUrl");
		assertEquals("newUrl", media.getUrl());
	}
	
	@Test
	public void testGetMediaType(){
		assertEquals("POSTER", media.getType());
		media.setType(MediaType.TRAILER);
		assertEquals("TRAILER", media.getType());
	}*/

}
