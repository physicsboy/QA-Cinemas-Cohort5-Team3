(function() {

    var GetBlockController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('getBlockController', ['$state','blockDal',GetBlockController]);
}());