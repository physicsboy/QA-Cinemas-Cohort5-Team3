(function() {

    var IncreaseRowController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('increaseRowController', ['$state','blockDal',IncreaseRowController]);
}());