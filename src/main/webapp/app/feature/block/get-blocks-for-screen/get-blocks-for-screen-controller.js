(function() {

    var GetBlocksForScreenController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('getBlocksForScreenController', ['$state','blockDal',GetBlocksForScreenController]);
}());