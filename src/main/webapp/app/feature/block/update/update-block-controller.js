(function() {

    var UpdateBlockController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('updateBlockController', ['$state','blockDal',UpdateBlockController]);
}());