(function() {

    var DeleteBlockController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('deleteBlockController', ['$state','blockDal',DeleteBlockController]);
}());