(function() {

    var DecreaseRowController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('decreaseRowController', ['$state','blockDal',DecreaseRowController]);
}());