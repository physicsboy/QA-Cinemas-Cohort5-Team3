(function() {

    var IncreaseColController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('increaseColController', ['$state','blockDal',IncreaseColController]);
}());