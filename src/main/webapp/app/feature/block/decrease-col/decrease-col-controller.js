(function() {

    var DecreaseColController =  function($state, blockDal) {
        var vm = this;

        vm.addBlock = function(blockToAdd) {
            
        };
    };

    angular.module('movieApp').controller('decreaseColController', ['$state','blockDal',DecreaseColController]);
}());

